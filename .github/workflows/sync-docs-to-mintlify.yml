name: Sync Docs to Mintlify

on:
  push:
    branches: [main]
    paths:
      - '**.md'
      - '.github/workflows/sync-docs.yml'

concurrency:
  group: sync-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout Source Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Enough for most comparisons

      - name: Checkout Target Docs Repository
        uses: actions/checkout@v4
        with:
          repository: andreisacal/mintlify-test
          token: ${{ secrets.DOCS_REPO_TOKEN }}
          path: docs-repo

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Detect and Validate Changed Files
        id: detect
        run: |
          # Get changed files
          CHANGED_FILES=$(git diff --name-only HEAD^ HEAD -- '*.md' 2>/dev/null || git ls-tree --name-only -r HEAD -- '*.md')
          
          # Validate markdown
          npm install -g markdownlint-cli
          VALIDATION_ERRORS=""
          
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              if ! markdownlint "$file" 2>/dev/null; then
                VALIDATION_ERRORS="${VALIDATION_ERRORS}Warning: $file has formatting issues\n"
              fi
            fi
          done <<< "$CHANGED_FILES"
          
          echo "changed_files<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "validation_errors<<EOF" >> $GITHUB_OUTPUT
          echo -e "$VALIDATION_ERRORS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Sync Files with Path Preservation
        id: sync
        run: |
          SYNCED_FILES=""
          FAILED_FILES=""
          
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              # Option 1: Mirror structure
              target_path="docs-repo/$file"
              mkdir -p "$(dirname "$target_path")"
              
              if cp "$file" "$target_path"; then
                SYNCED_FILES="${SYNCED_FILES}✅ $file\n"
              else
                FAILED_FILES="${FAILED_FILES}❌ $file\n"
              fi
            fi
          done <<< "${{ steps.detect.outputs.changed_files }}"
          
          echo "synced_files<<EOF" >> $GITHUB_OUTPUT
          echo -e "$SYNCED_FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "failed_files<<EOF" >> $GITHUB_OUTPUT
          echo -e "$FAILED_FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.DOCS_REPO_TOKEN }}
          path: docs-repo
          commit-message: "docs: sync from main repository"
          branch: auto-sync/${{ github.sha }}
          delete-branch: true
          title: "📚 Sync Documentation from Main Repository"
          body: |
            ## Documentation Sync
            
            **Source:** ${{ github.repository }}@${{ github.sha }}
            **Triggered by:** ${{ github.actor }}
            
            ### Changed Files
            ${{ steps.sync.outputs.synced_files }}
            
            ### Validation Warnings
            ${{ steps.detect.outputs.validation_errors }}
            
            ### Failed Files
            ${{ steps.sync.outputs.failed_files }}
            
            ---
            *This PR was automatically generated by the docs sync workflow.*

      - name: Job Summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 📚 Documentation Sync Summary
          
          ## Synced Files
          ${{ steps.sync.outputs.synced_files }}
          
          ## Validation Issues
          ${{ steps.detect.outputs.validation_errors }}
          
          ## Failed Files
          ${{ steps.sync.outputs.failed_files }}
          EOF